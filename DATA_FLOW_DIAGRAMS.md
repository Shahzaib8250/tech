# TECNO TRIBE Survey - Data Flow Diagrams

## 📊 Visual Data Flow Documentation

This document provides detailed visual representations of how data flows through the TECNO TRIBE Survey Application.

---

## 1. Complete User Journey Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY MAP                            │
└─────────────────────────────────────────────────────────────────────┘

START: User scans QR code or visits URL
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  HOMEPAGE                                                         │
│  • Welcome message                                                │
│  • Survey description (3-5 minutes)                               │
│  • TECNO logo and tagline                                         │
│  • Start Feedback button                                          │
│                                                                   │
│  USER ACTION: Clicks "Start Feedback"                             │
│  SYSTEM ACTION:                                                   │
│    → Clear any previous survey data (startFreshSurvey())          │
│    → Reset localStorage                                           │
│    → Navigate to /survey/1                                        │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 1: Basic Information                                │
│  Fields:                                                          │
│    • Gender (required)                                            │
│    • Year of Study (required)                                     │
│    • Field of Study (required, "other" option)                    │
│    • University (required, "other" option)                        │
│                                                                   │
│  USER INTERACTION:                                                │
│    For each field change:                                         │
│      → onChange(fieldName, value)                                 │
│      → updateSurveyData() in Zustand store                        │
│      → Automatic save to localStorage via persist middleware      │
│                                                                   │
│  NAVIGATION:                                                      │
│    Back → Returns to HomePage                                     │
│    Next → Validates all fields                                    │
│           ├─[VALID]─→ Navigate to /survey/2                       │
│           └─[INVALID]─→ Show alert, remain on page                │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 2: Social Media Habits                              │
│  Fields:                                                          │
│    • Social media platforms used (multi-select, required)         │
│    • Time spent daily (required)                                  │
│    • Follow tech content frequency (required)                     │
│    • Tech update sources (multi-select, required)                 │
│                                                                   │
│  DATA FLOW: Same as Page 1                                        │
│  VALIDATION:                                                      │
│    → At least 1 platform selected                                 │
│    → At least 1 tech source selected                              │
│    → All single-select fields filled                              │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 3: Mobile Phone Usage                               │
│  Fields:                                                          │
│    • Current phone brand (required, "other" option)               │
│    • Top 3 phone functions (exactly 3, required)                  │
│    • Change frequency (required)                                  │
│    • TECNO experience (required)                                  │
│    • Rating (conditional: only if "used TECNO")                   │
│                                                                   │
│  CONDITIONAL LOGIC:                                               │
│    IF tecnoExperience === "yes-used":                             │
│      → Show tecnoExperienceRating field                           │
│      → Validate rating is selected                                │
│    ELSE:                                                          │
│      → Hide rating field                                          │
│      → No rating validation                                       │
│                                                                   │
│  SPECIAL VALIDATION:                                              │
│    → topPhoneFunctions.length === 3 (exactly 3, not more/less)   │
│    → MaxSelections enforced in UI (disables after 3)              │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 4: Skills & Work                                    │
│  Fields:                                                          │
│    • Learning skills (multi-select, required, "none" option)      │
│    • Part-time work (multi-select, required, "none" option)       │
│                                                                   │
│  USER INSIGHT:                                                    │
│    Both fields allow "none" selection, indicating the field is    │
│    still required but user can indicate "not applicable"          │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 5: What Matters Most in a New Phone                │
│  Fields:                                                          │
│    • Phone features ranking (rank 8 features from 1-8, required)  │
│    • Budget range (required)                                      │
│    • Preferred colors (multi-select, required)                    │
│                                                                   │
│  COMPLEX VALIDATION:                                              │
│    phoneFeaturesRanking validation:                               │
│      → Must have exactly 8 rankings                               │
│      → Each feature must have a rank (1-8)                        │
│      → No duplicate ranks (Set check for uniqueness)              │
│      → All ranks must be present                                  │
│                                                                   │
│  RANKING LOGIC:                                                   │
│    When user selects a rank for a feature:                        │
│      → Remove that rank from any other feature                    │
│      → Assign rank to selected feature                            │
│      → Disable already-used ranks in other dropdowns              │
│                                                                   │
│  COLOR PICKER:                                                    │
│    → Visual color blocks with hex codes                           │
│    → Multiple selection allowed                                   │
│    → At least 1 color required                                    │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 6: TECNO Campus Brand Ambassador Program           │
│  OPTIONAL SECTION - Can be skipped                               │
│                                                                   │
│  Initial Display:                                                 │
│    Question: "Would you like to become a TECNO Campus            │
│               Ambassador?"                                        │
│    Options: [Yes] [Skip →]                                        │
│                                                                   │
│  DECISION TREE:                                                   │
│                                                                   │
│    User clicks "Skip":                                            │
│      → Set interestedInAmbassador = "skipped"                     │
│      → Navigate to /survey/8 (Suggestions page)                   │
│      → SKIP pages 7 (Contact Info)                                │
│                                                                   │
│    User clicks "Yes":                                             │
│      → Set interestedInAmbassador = "yes"                         │
│      → Show additional fields:                                    │
│         - Ambassador strengths (multi-select, required)           │
│         - Ambassador benefits (multi-select, required)            │
│      → Next button appears                                        │
│      → Validation required for both new fields                    │
│      → Navigate to /survey/7 (Contact Info) on Next               │
│                                                                   │
│  IMPORTANT NOTE:                                                  │
│    Contact information (Page 7) is ONLY collected if user         │
│    is interested in ambassador program. This reduces friction     │
│    for general survey participants.                               │
└──────────────────────────────────────────────────────────────────┘
  │
  ├────[Skipped]────┐
  │                 │
  │                 ▼
  │           ┌─────────────────────────────────────────────┐
  │           │  JUMP TO PAGE 8                             │
  │           │  (Skip Contact Info)                        │
  │           └─────────────────────────────────────────────┘
  │                 │
  │                 │
  ▼                 │
┌────────────────────────────────────────────────────────────┐     │
│  SURVEY PAGE 7: Contact Information                       │     │
│  CONDITIONAL PAGE - Only shown if interested in ambassador │     │
│                                                            │     │
│  Fields:                                                   │     │
│    • Name (text, required)                                 │     │
│    • Contact number (tel, required)                        │     │
│    • Most active social media platform (required)          │     │
│    • Social media profile link (URL, OPTIONAL)             │     │
│    • Follower count (dropdown, required)                   │     │
│                                                            │     │
│  WHY THIS DATA:                                            │     │
│    TECNO needs contact info to reach out to potential      │     │
│    ambassadors. Social media metrics help gauge influence. │     │
│                                                            │     │
│  PRIVACY NOTE:                                             │     │
│    This data is only collected for ambassador candidates.  │     │
│    Regular survey participants remain anonymous.           │     │
└────────────────────────────────────────────────────────────┘     │
  │                                                                │
  │◄───────────────────────────────────────────────────────────────┘
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  SURVEY PAGE 8: Suggestions                                      │
│  OPTIONAL SECTION - No validation                                │
│                                                                   │
│  Field:                                                           │
│    • Suggestions (textarea, optional)                             │
│                                                                   │
│  USER ACTION: Clicks "Submit"                                     │
│                                                                   │
│  SUBMISSION FLOW:                                                 │
│    1. handleSubmit() triggered                                    │
│    2. Final validation (always passes - suggestions optional)     │
│    3. saveProgress() - ensure latest data saved                   │
│    4. submitSurvey() in Zustand store                             │
│       │                                                           │
│       ├─→ Collect all surveyData from store                       │
│       │                                                           │
│       ├─→ POST to /.netlify/functions/submit-survey              │
│       │   Headers: Content-Type: application/json                │
│       │   Body: { ...surveyData } (all fields)                    │
│       │                                                           │
│       ├─→ [BACKEND AVAILABLE]                                     │
│       │   ├─→ Response 200: { success: true, data: {...} }       │
│       │   ├─→ Call resetSurvey()                                 │
│       │   ├─→ Clear localStorage('survey-storage')               │
│       │   └─→ Navigate to /thank-you                             │
│       │                                                           │
│       └─→ [BACKEND UNAVAILABLE] (404, ECONNREFUSED, ERR_NETWORK) │
│           ├─→ Create fallback object:                            │
│           │   {                                                  │
│           │     ...surveyData,                                   │
│           │     submittedAt: new Date().toISOString(),           │
│           │     fallback: true,                                  │
│           │     id: `fallback_${Date.now()}`                     │
│           │   }                                                  │
│           ├─→ Save to localStorage('survey-submissions')         │
│           ├─→ Call resetSurvey()                                 │
│           ├─→ Clear localStorage('survey-storage')               │
│           └─→ Navigate to /thank-you (with fallback message)     │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│  THANK YOU PAGE                                                   │
│                                                                   │
│  Display:                                                         │
│    • Success checkmark icon                                       │
│    • Thank you message                                            │
│    • Next steps:                                                  │
│      1. Show proof to claim gift at TECNO booth                   │
│      2. Join Facebook fan community                               │
│      3. Watch for campus activities                               │
│    • "Start New Survey" button                                    │
│                                                                   │
│  USER ACTION: Click "Start New Survey"                            │
│    → resetSurvey()                                                │
│    → Navigate to / (HomePage)                                     │
│    → Ready for next participant                                   │
│                                                                   │
│  BOOTH WORKFLOW:                                                  │
│    User shows this page to booth staff as proof of completion     │
│    Staff verifies completion and provides gift/incentive          │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
END: User receives gift, survey cycle complete

```

---

## 2. State Management Data Flow

```
┌───────────────────────────────────────────────────────────────────┐
│                   ZUSTAND STORE LIFECYCLE                         │
└───────────────────────────────────────────────────────────────────┘

APP INITIALIZATION
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  1. App loads in browser                                    │
│     index.js → ReactDOM.render(<App />)                     │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Zustand store initializes                               │
│     surveyStore.js imports and creates store                │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Persist middleware checks localStorage                  │
│     Key: 'survey-storage'                                   │
│                                                             │
│     CASE A: localStorage has data                           │
│       → Rehydrate store with saved data                     │
│       → User can continue previous survey                   │
│       → currentPage restored to last page                   │
│                                                             │
│     CASE B: localStorage is empty                           │
│       → Use initial state values                            │
│       → Fresh survey ready                                  │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Component mounts and subscribes to store                │
│     const { surveyData, updateSurveyData } =                │
│       useSurveyStore()                                      │
│                                                             │
│     → Component receives current state                      │
│     → Component can call store actions                      │
└─────────────────────────────────────────────────────────────┘

USER INTERACTION FLOW
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  User types in form field                                   │
│    Input: <input onChange={e => onChange('name', e.value)} />│
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  onChange handler in SurveyPage                             │
│    handleDataChange(fieldName, value)                       │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  Zustand action: updateSurveyData                           │
│    updateSurveyData: (data) => set((state) => ({            │
│      surveyData: { ...state.surveyData, ...data }           │
│    }))                                                      │
│                                                             │
│  Example: updateSurveyData({ name: "John" })                │
│    Before: { surveyData: { name: '', age: 25 } }            │
│    After:  { surveyData: { name: 'John', age: 25 } }        │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  Zustand notifies all subscribers                           │
│    → All components using this state re-render              │
│    → Input field shows new value                            │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  Persist middleware triggers                                │
│    → Serializes state                                       │
│    → Only includes partialize selection:                    │
│      - sessionId                                            │
│      - sessionStatus                                        │
│      - surveyData                                           │
│      - currentPage                                          │
│    → Saves to localStorage('survey-storage')                │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  localStorage updated                                       │
│    localStorage.setItem('survey-storage', JSON.stringify({  │
│      state: {                                               │
│        sessionId: null,                                     │
│        sessionStatus: 'inactive',                           │
│        surveyData: { name: 'John', ... },                   │
│        currentPage: 2                                       │
│      },                                                     │
│      version: 0                                             │
│    }))                                                      │
└─────────────────────────────────────────────────────────────┘

NAVIGATION FLOW
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  User clicks "Next" button                                  │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  handleNext() in SurveyPage                                 │
│    1. Run isPageValid() validation                          │
│    2. If valid, call saveProgress()                         │
│    3. Navigate to next page via React Router                │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  React Router updates URL                                   │
│    /survey/2 → /survey/3                                    │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  SurveyPage re-renders with new page param                  │
│    useParams() → { page: '3' }                              │
│    currentPage = parseInt('3') - 1 = 2                      │
│    renderForm() → returns form for page 2                   │
└─────────────────────────────────────────────────────────────┘

SUBMISSION FLOW
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  User clicks "Submit" on final page                         │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  handleSubmit() in SurveyPage                               │
│    1. Set isSubmitting = true (disable button)              │
│    2. Validate final page                                   │
│    3. Call saveProgress()                                   │
│    4. Call submitSurvey() from store                        │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  submitSurvey() action in Zustand store                     │
│                                                             │
│  const surveyData = get().surveyData;                       │
│  console.log('Submitting:', surveyData);                    │
│                                                             │
│  try {                                                      │
│    const response = await axios.post(                       │
│      '/.netlify/functions/submit-survey',                   │
│      surveyData                                             │
│    );                                                       │
│                                                             │
│    if (response.data.success) {                             │
│      get().resetSurvey(); // Clear store                    │
│      localStorage.removeItem('survey-storage');             │
│      return { success: true, data: response.data };         │
│    }                                                        │
│  } catch (error) {                                          │
│    // Fallback mechanism (see next diagram)                │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  Navigate to /thank-you                                     │
│  ThankYouPage displays success message                      │
└─────────────────────────────────────────────────────────────┘

```

---

## 3. API Submission Flow with Fallback

```
┌───────────────────────────────────────────────────────────────────┐
│              SURVEY SUBMISSION WITH FALLBACK LOGIC                │
└───────────────────────────────────────────────────────────────────┘

User clicks "Submit"
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  handleSubmit() in SurveyPage.js                            │
│    await submitSurvey()                                     │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  submitSurvey() in surveyStore.js                           │
│                                                             │
│  Prepare request:                                           │
│    URL: /.netlify/functions/submit-survey                   │
│    Method: POST                                             │
│    Headers: { Content-Type: application/json }              │
│    Body: {                                                  │
│      gender: "male",                                        │
│      yearOfStudy: "third-year",                             │
│      fieldOfStudy: "engineering",                           │
│      university: "uol",                                     │
│      socialMediaPlatforms: ["instagram", "tiktok"],         │
│      ... (all survey fields)                                │
│      suggestions: "Great initiative!"                       │
│    }                                                        │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  axios.post() sends HTTP request                            │
└─────────────────────────────────────────────────────────────┘
  │
  ├─────────[SUCCESS PATH]─────────┐
  │                                  │
  ▼                                  ▼
[BACKEND AVAILABLE]              [BACKEND UNAVAILABLE]
  │                                  │
  ▼                                  ▼
┌────────────────────────┐      ┌─────────────────────────────┐
│  Backend responds      │      │  Network error caught        │
│  200 OK                │      │  Possible error codes:       │
│  {                     │      │    - ECONNREFUSED           │
│    success: true,      │      │    - ERR_NETWORK            │
│    message: "Saved",   │      │    - 404 Not Found          │
│    data: {             │      │    - Timeout                │
│      id: 12345,        │      └─────────────────────────────┘
│      timestamp: ...    │         │
│    }                   │         ▼
│  }                     │      ┌─────────────────────────────────────┐
└────────────────────────┘      │  Fallback mechanism activates       │
  │                             │                                     │
  ▼                             │  console.log('Backend not available,│
┌────────────────────────┐      │   saving to localStorage...');      │
│  Success handler       │      │                                     │
│                        │      │  Create fallback data:              │
│  console.log('✅       │      │    const fallbackData = {           │
│    Survey submitted'); │      │      ...get().surveyData,           │
│                        │      │      submittedAt: new Date()        │
│  // Clear local data  │      │        .toISOString(),              │
│  get().resetSurvey();  │      │      fallback: true,                │
│  localStorage          │      │      id: `fallback_${Date.now()}`   │
│    .removeItem(        │      │    };                               │
│      'survey-storage'  │      │                                     │
│    );                  │      │  // Save to fallback storage        │
│                        │      │  const existing = JSON.parse(       │
│  return {              │      │    localStorage.getItem(            │
│    success: true,      │      │      'survey-submissions'           │
│    data: response.data │      │    ) || '[]'                        │
│  };                    │      │  );                                 │
└────────────────────────┘      │  existing.push(fallbackData);       │
  │                             │  localStorage.setItem(              │
  │                             │    'survey-submissions',            │
  │                             │    JSON.stringify(existing)         │
  │                             │  );                                 │
  │                             │                                     │
  │                             │  // Still clear survey data         │
  │                             │  get().resetSurvey();               │
  │                             │  localStorage.removeItem(           │
  │                             │    'survey-storage'                 │
  │                             │  );                                 │
  │                             │                                     │
  │                             │  return {                           │
  │                             │    success: true,  // Still success!│
  │                             │    data: fallbackData,              │
  │                             │    message: 'Survey saved locally',│
  │                             │    fallback: true                   │
  │                             │  };                                 │
  │                             └─────────────────────────────────────┘
  │                                │
  └────────────────────────────────┘
                │
                ▼
┌───────────────────────────────────────────────────────────────┐
│  Back in handleSubmit() in SurveyPage                         │
│                                                               │
│  const result = await submitSurvey();                         │
│  if (result.success) {                                        │
│    if (result.fallback) {                                     │
│      // Optional: Show user a specific message                │
│      alert(`Survey saved! ${result.message}`);                │
│    }                                                          │
│    navigate('/thank-you');                                    │
│  }                                                            │
└───────────────────────────────────────────────────────────────┘
                │
                ▼
┌───────────────────────────────────────────────────────────────┐
│  User sees Thank You page                                     │
│  (Same experience whether backend was available or not)       │
└───────────────────────────────────────────────────────────────┘

FALLBACK DATA STRUCTURE IN localStorage:

localStorage['survey-submissions'] = [
  {
    // First submission (fallback)
    gender: "female",
    yearOfStudy: "second-year",
    ... all survey fields ...,
    submittedAt: "2024-01-15T10:30:00.000Z",
    fallback: true,
    id: "fallback_1705317000000"
  },
  {
    // Second submission (fallback)
    gender: "male",
    yearOfStudy: "fourth-year",
    ... all survey fields ...,
    submittedAt: "2024-01-15T14:20:00.000Z",
    fallback: true,
    id: "fallback_1705331200000"
  }
  // ... more submissions
]

RETRY MECHANISM (Future Feature):

When backend comes back online, admin can call:
  retryFallbackSubmissions()
    │
    ├─→ Gets all submissions with fallback: true
    ├─→ Attempts POST for each submission
    ├─→ On success: removes from localStorage
    ├─→ On failure: keeps in localStorage for next retry
    └─→ Returns summary: { success: 2, failed: 0 }

```

---

## 4. Validation Flow

```
┌───────────────────────────────────────────────────────────────────┐
│                    VALIDATION ARCHITECTURE                        │
└───────────────────────────────────────────────────────────────────┘

VALIDATION TIMING:
  • NO real-time validation (user can type freely)
  • Validation occurs on navigation (Next/Submit button)
  • Failed validation blocks navigation

┌─────────────────────────────────────────────────────────────┐
│  User clicks "Next" or "Submit"                             │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  handleNext() or handleSubmit()                             │
│    const valid = isPageValid();                             │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  isPageValid() - Switch statement based on currentPage      │
└─────────────────────────────────────────────────────────────┘
  │
  ├─ Page 0 ──────────────────────────────────────────────────┐
  │                                                            │
  │  const { gender, yearOfStudy, fieldOfStudy,               │
  │          university } = surveyData;                        │
  │                                                            │
  │  return !!(gender && yearOfStudy &&                        │
  │            fieldOfStudy && university);                    │
  │                                                            │
  │  Logic: Truthy check for all 4 fields                     │
  │    Empty string = false                                    │
  │    Any value = true                                        │
  │    Double negation (!!) converts to boolean                │
  │                                                            │
  ├─ Page 1 ──────────────────────────────────────────────────┤
  │                                                            │
  │  const { socialMediaPlatforms = [],                        │
  │          timeSpentOnSocialMedia,                           │
  │          followsTechContent,                               │
  │          techUpdateSources = [] } = surveyData;            │
  │                                                            │
  │  return socialMediaPlatforms.length > 0 &&                 │
  │         !!timeSpentOnSocialMedia &&                        │
  │         !!followsTechContent &&                            │
  │         techUpdateSources.length > 0;                      │
  │                                                            │
  │  Logic: Arrays must have at least 1 item                   │
  │         Single-select fields must be filled                │
  │                                                            │
  ├─ Page 2 ──────────────────────────────────────────────────┤
  │                                                            │
  │  const { currentPhoneBrand,                                │
  │          topPhoneFunctions = [],                           │
  │          phoneChangeFrequency,                             │
  │          tecnoExperience,                                  │
  │          tecnoExperienceRating } = surveyData;             │
  │                                                            │
  │  // Base validation                                        │
  │  const baseOk = !!currentPhoneBrand &&                     │
  │                 topPhoneFunctions.length === 3 &&          │
  │                 !!phoneChangeFrequency &&                  │
  │                 !!tecnoExperience;                         │
  │                                                            │
  │  // Conditional validation                                 │
  │  const ratingOk = tecnoExperience !== 'yes-used' ||        │
  │                   !!tecnoExperienceRating;                 │
  │                                                            │
  │  return baseOk && ratingOk;                                │
  │                                                            │
  │  Logic: EXACTLY 3 functions required                       │
  │         Rating required ONLY if user has used TECNO        │
  │         Conditional: (NOT used) OR (used AND rated)        │
  │                                                            │
  ├─ Page 3 ──────────────────────────────────────────────────┤
  │                                                            │
  │  const { learningSkills = [],                              │
  │          partTimeWork = [] } = surveyData;                 │
  │                                                            │
  │  return learningSkills.length > 0 &&                       │
  │         partTimeWork.length > 0;                           │
  │                                                            │
  │  Logic: Both arrays need at least 1 selection              │
  │         "None" option counts as a selection                │
  │                                                            │
  ├─ Page 4 ──────────────────────────────────────────────────┤
  │                                                            │
  │  const { phoneFeaturesRanking = {},                        │
  │          phoneBudget,                                      │
  │          preferredPhoneColors = [] } = surveyData;         │
  │                                                            │
  │  // Extract ranking values                                 │
  │  const rankingValues = Object.values(                      │
  │    phoneFeaturesRanking || {}                              │
  │  );                                                        │
  │                                                            │
  │  // Check: 8 items ranked, all unique ranks                │
  │  const hasAllEight = rankingValues.length === 8 &&         │
  │                      new Set(rankingValues).size === 8;    │
  │                                                            │
  │  return hasAllEight && !!phoneBudget &&                    │
  │         preferredPhoneColors.length > 0;                   │
  │                                                            │
  │  Logic: Complex ranking validation                         │
  │    1. Must have exactly 8 rankings                         │
  │    2. Set ensures no duplicate ranks                       │
  │    3. If Set size === array length, all unique             │
  │                                                            │
  │  Example Valid:                                            │
  │    { feat1: '1', feat2: '2', ... feat8: '8' }              │
  │    Values: ['1','2','3','4','5','6','7','8']               │
  │    Set: {'1','2','3','4','5','6','7','8'} → size 8 ✓       │
  │                                                            │
  │  Example Invalid:                                          │
  │    { feat1: '1', feat2: '1', feat3: '2' ... }              │
  │    Values: ['1','1','2','3','4','5','6','7']               │
  │    Set: {'1','2','3','4','5','6','7'} → size 7 ✗           │
  │                                                            │
  ├─ Page 5 ──────────────────────────────────────────────────┤
  │                                                            │
  │  if (surveyData.interestedInAmbassador === 'yes') {        │
  │    const { ambassadorStrengths = [],                       │
  │            ambassadorBenefits = [] } = surveyData;         │
  │    return ambassadorStrengths.length > 0 &&                │
  │           ambassadorBenefits.length > 0;                   │
  │  }                                                         │
  │  return true; // Skip is allowed                           │
  │                                                            │
  │  Logic: Conditional validation                             │
  │    IF interested === 'yes':                                │
  │      THEN validate strengths AND benefits                  │
  │    ELSE (skipped or empty):                                │
  │      THEN no validation, allow proceed                     │
  │                                                            │
  ├─ Page 6 ──────────────────────────────────────────────────┤
  │                                                            │
  │  const { name, contactNumber,                              │
  │          socialMediaPlatform,                              │
  │          followerCount } = surveyData;                     │
  │                                                            │
  │  return !!name && !!contactNumber &&                       │
  │         !!socialMediaPlatform && !!followerCount;          │
  │                                                            │
  │  Logic: All 4 fields required                              │
  │    Note: socialMediaLink is NOT validated                  │
  │          (intentionally optional)                          │
  │                                                            │
  └─ Page 7 ──────────────────────────────────────────────────┤
                                                               │
    return true; // No validation                              │
                                                               │
    Logic: Suggestions are completely optional                 │
           User can submit empty                               │
───────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  Validation result returned (true/false)                    │
└─────────────────────────────────────────────────────────────┘
  │
  ├─[TRUE]──────────────┐
  │                      │
  ▼                      ▼
VALID                INVALID
  │                      │
  ▼                      ▼
Proceed to         ┌──────────────────────────────────┐
next page          │  Show alert:                     │
or submit          │  "Please complete all required   │
                   │   fields before continuing."     │
                   │                                  │
                   │  User remains on current page    │
                   │  Can correct and retry           │
                   └──────────────────────────────────┘

```

---

## 5. localStorage Structure

```
┌───────────────────────────────────────────────────────────────────┐
│              localStorage DATA ARCHITECTURE                       │
└───────────────────────────────────────────────────────────────────┘

Browser localStorage contains TWO keys:

1. 'survey-storage' (Active Survey Data)
2. 'survey-submissions' (Fallback Submissions)

─────────────────────────────────────────────────────────────────────

KEY 1: 'survey-storage'
Purpose: Store current survey progress
Managed by: Zustand persist middleware
Lifecycle: Created on first input, cleared on submission

Structure:
{
  "state": {
    "sessionId": null,
    "sessionStatus": "inactive",
    "currentPage": 3,
    "surveyData": {
      "gender": "male",
      "yearOfStudy": "third-year",
      "fieldOfStudy": "engineering",
      "fieldOfStudyOther": "",
      "university": "uol",
      "universityOther": "",
      "socialMediaPlatforms": ["instagram", "tiktok"],
      "socialMediaPlatformsOther": "",
      "timeSpentOnSocialMedia": "4-5-hours",
      "followsTechContent": "often",
      "techUpdateSources": ["influencers", "tech-websites"],
      "techUpdateSourcesOther": "",
      "currentPhoneBrand": "samsung",
      "currentPhoneBrandOther": "",
      "topPhoneFunctions": [
        "camera-video",
        "social-media",
        "communication"
      ],
      "topPhoneFunctionsOther": "",
      "phoneChangeFrequency": "1-2-years",
      "tecnoExperience": "heard-of",
      "tecnoExperienceRating": "",
      "learningSkills": ["web-development", "programming"],
      "learningSkillsOther": "",
      "partTimeWork": ["none"],
      "partTimeWorkOther": "",
      "phoneFeaturesRanking": {
        "intelligent-camera": "1",
        "long-battery": "2",
        "fast-charging": "3",
        "high-display": "4",
        "high-performance": "5",
        "slim-design": "6",
        "durable": "7",
        "ai-features": "8"
      },
      "phoneBudget": "46-60k",
      "preferredPhoneColors": [
        "wisteria-purple",
        "sea-green",
        "carbon"
      ],
      "interestedInAmbassador": "yes",
      "ambassadorStrengths": [
        "large-social-circle",
        "content-creation"
      ],
      "ambassadorStrengthsOther": "",
      "ambassadorBenefits": [
        "free-trial",
        "merchandise"
      ],
      "ambassadorBenefitsOther": "",
      "name": "John Doe",
      "contactNumber": "+923001234567",
      "socialMediaPlatform": "instagram",
      "socialMediaPlatformOther": "",
      "socialMediaLink": "https://instagram.com/johndoe",
      "followerCount": "1000-5000",
      "suggestions": "Great survey experience!"
    }
  },
  "version": 0
}

Use Cases:
  • User closes browser → Data persists
  • User returns → Continue from last page
  • User refreshes page → No data loss
  • Submit successful → This key is deleted
  • Start new survey → This key is deleted

─────────────────────────────────────────────────────────────────────

KEY 2: 'survey-submissions'
Purpose: Store fallback submissions when backend unavailable
Managed by: Custom code in submitSurvey()
Lifecycle: Created on first fallback, accumulates submissions

Structure (Array of submissions):
[
  {
    "id": "fallback_1705317000000",
    "submittedAt": "2024-01-15T10:30:00.000Z",
    "fallback": true,
    "gender": "female",
    "yearOfStudy": "second-year",
    "fieldOfStudy": "business",
    "university": "ucp",
    ... (all survey fields) ...
    "suggestions": "Looking forward to TECNO events!"
  },
  {
    "id": "fallback_1705320600000",
    "submittedAt": "2024-01-15T11:30:00.000Z",
    "fallback": true,
    "gender": "male",
    "yearOfStudy": "fourth-year",
    "fieldOfStudy": "engineering",
    "university": "bnu",
    ... (all survey fields) ...
    "suggestions": ""
  }
  // More submissions...
]

Special Fields:
  • id: Unique identifier (timestamp-based)
  • submittedAt: ISO 8601 timestamp
  • fallback: true (flag indicating needs retry)
  • All original survey fields included

Use Cases:
  • Backend down during submission → Save here
  • Multiple users submitting while offline → Array grows
  • Admin retries → Successfully submitted items removed
  • Backend never comes back → Admin exports manually

Retry Logic (retryFallbackSubmissions):
  1. Load array from localStorage
  2. Filter items with fallback: true
  3. For each item:
     a. Remove fallback flag
     b. POST to backend
     c. If success: remove from array
     d. If fail: keep in array
  4. Save updated array back to localStorage
  5. Return summary: { success: 2, failed: 1 }

─────────────────────────────────────────────────────────────────────

STORAGE SIZE ESTIMATION:

Single Survey (survey-storage):
  • Typical size: 2-4 KB
  • Maximum size: ~6 KB (with long suggestions)
  • localStorage limit: 5-10 MB (browser dependent)
  • Capacity: ~2,500 surveys (conservative)

Fallback Submissions (survey-submissions):
  • Single entry: 2-4 KB
  • 100 submissions: ~300 KB
  • 1000 submissions: ~3 MB
  • Recommended max: 500 submissions before export

Total Usage:
  Current survey: 4 KB
  500 fallback submissions: 2 MB
  Total: ~2 MB (well within limits)

─────────────────────────────────────────────────────────────────────

DEBUGGING COMMANDS (Browser Console):

// View active survey data
JSON.parse(localStorage.getItem('survey-storage'))

// View fallback submissions
JSON.parse(localStorage.getItem('survey-submissions'))

// Clear active survey
localStorage.removeItem('survey-storage')

// Clear fallback submissions
localStorage.removeItem('survey-submissions')

// Clear everything
localStorage.clear()

// Check storage usage
console.log(
  'Active:', 
  (localStorage.getItem('survey-storage') || '').length, 
  'bytes'
);
console.log(
  'Fallback:', 
  (localStorage.getItem('survey-submissions') || '').length, 
  'bytes'
);

```

---

## 6. Component Communication Flow

```
┌───────────────────────────────────────────────────────────────────┐
│              COMPONENT COMMUNICATION DIAGRAM                      │
└───────────────────────────────────────────────────────────────────┘

                     ┌────────────────┐
                     │    App.js      │
                     │  (Root Router) │
                     └────────┬───────┘
                              │
                 ┌────────────┼────────────┐
                 │            │            │
                 ▼            ▼            ▼
         ┌─────────────┐ ┌──────────┐ ┌──────────────┐
         │  HomePage   │ │ Survey   │ │  ThankYou    │
         │             │ │  Page    │ │    Page      │
         └─────────────┘ └────┬─────┘ └──────────────┘
                              │
                              │ useParams({ page })
                              │ useSurveyStore()
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ▼                    ▼                    ▼
    ┌──────────┐       ┌──────────┐         ┌──────────┐
    │  Basic   │       │  Social  │   ...   │ Suggest  │
    │   Info   │       │  Media   │         │  -ions   │
    │   Form   │       │   Form   │         │   Form   │
    └────┬─────┘       └────┬─────┘         └────┬─────┘
         │                  │                     │
         └──────────────────┼─────────────────────┘
                            │
                            │ Props: { data, onChange }
                            │
         ┌──────────────────┼────────────────────┐
         │                  │                    │
         ▼                  ▼                    ▼
    ┌──────────┐      ┌──────────┐        ┌──────────┐
    │ Dropdown │      │  Multi   │        │  Color   │
    │   With   │      │ Dropdown │        │  Picker  │
    │  Other   │      │   With   │        │          │
    │          │      │  Other   │        │          │
    └──────────┘      └──────────┘        └──────────┘
         │                  │                    │
         │ Props: { label, options, value, onChange, ... }
         │
         └──────────────────┼─────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  Form Input   │
                    │   (Native)    │
                    └───────────────┘

DATA FLOW (Bottom-Up):

1. User types in native input
      │
      ▼
2. Input onChange fires
      │
      ▼
3. FormComponent handler (e.g., DropdownWithOther)
   Updates local state if needed
   Calls props.onChange(name, value)
      │
      ▼
4. Form component handler (e.g., BasicInfoForm)
   Receives onChange from props
   Calls props.onChange(fieldName, value)
      │
      ▼
5. SurveyPage handleDataChange(fieldName, value)
   Calls updateSurveyData({ [fieldName]: value })
      │
      ▼
6. Zustand store updateSurveyData action
   Merges new data into surveyData
   Triggers re-render
   Persist middleware saves to localStorage
      │
      ▼
7. All subscribed components re-render with new data

PROPS FLOW (Top-Down):

App.js
  └─→ SurveyPage
        ├─→ data = useSurveyStore(state => state.surveyData)
        ├─→ onChange = handleDataChange
        └─→ BasicInfoForm
              ├─→ data={surveyData}
              ├─→ onChange={handleDataChange}
              └─→ DropdownWithOther
                    ├─→ label="Gender"
                    ├─→ value={data.gender}
                    ├─→ onChange={onChange}
                    └─→ name="gender"

ZUSTAND STORE (Global State Bus):

All components can access store independently:

HomePage:
  const { startFreshSurvey } = useSurveyStore();
  
SurveyPage:
  const { surveyData, updateSurveyData, submitSurvey } = 
    useSurveyStore();
    
ThankYouPage:
  const { resetSurvey } = useSurveyStore();

No prop drilling needed for global actions!

```

---

## 7. Error Handling & Edge Cases

```
┌───────────────────────────────────────────────────────────────────┐
│              ERROR HANDLING & EDGE CASES                          │
└───────────────────────────────────────────────────────────────────┘

SCENARIO 1: User refreshes browser mid-survey
  ├─→ Zustand persist middleware rehydrates state
  ├─→ surveyData and currentPage restored
  ├─→ React Router loads correct URL
  ├─→ User continues from where they left off
  └─→ ✅ No data loss

SCENARIO 2: User clicks browser back button
  ├─→ React Router detects URL change
  ├─→ SurveyPage re-renders with new page param
  ├─→ Data still in Zustand store
  ├─→ Previous form shows with saved data
  └─→ ✅ Seamless navigation

SCENARIO 3: User clicks browser forward button
  ├─→ Validation NOT run (only on Next button)
  ├─→ User can view future pages
  ├─→ Data may be incomplete
  ├─→ Submit button validation will catch it
  └─→ ⚠️ Edge case: User can preview pages

SCENARIO 4: User opens survey in multiple tabs
  ├─→ Both tabs share same localStorage
  ├─→ Changes in Tab 1 update localStorage
  ├─→ Tab 2 does NOT auto-refresh
  ├─→ If Tab 2 submits, it has stale data
  └─→ ⚠️ Potential issue: Race condition

SCENARIO 5: localStorage quota exceeded
  ├─→ Zustand persist middleware catches error
  ├─→ Store continues to work (in-memory)
  ├─→ Data NOT persisted
  ├─→ Refresh would lose data
  └─→ ⚠️ Silent failure, no user notification

SCENARIO 6: Backend returns 500 error
  ├─→ Axios throws error
  ├─→ NOT caught by fallback mechanism
  │   (fallback only for network errors)
  ├─→ submitSurvey returns { success: false, error: ... }
  ├─→ User sees error alert
  ├─→ Can retry submission
  └─→ ✅ Error shown to user

SCENARIO 7: User disables JavaScript
  ├─→ React app does not load
  ├─→ Blank page or noscript warning
  ├─→ No fallback HTML form
  └─→ ❌ Complete failure (expected for React apps)

SCENARIO 8: User has slow network
  ├─→ Form loads normally (static assets cached)
  ├─→ Submit shows "Submitting..." button state
  ├─→ Long wait time
  ├─→ Eventually: Success or timeout error
  └─→ ⚠️ No progress indicator during submit

SCENARIO 9: User submits, then backs to survey
  ├─→ Data has been cleared by resetSurvey()
  ├─→ Empty form loads
  ├─→ User would need to re-enter everything
  └─→ ✅ Intended behavior (fresh start)

SCENARIO 10: User enters invalid data (e.g., emoji in name)
  ├─→ No input validation or sanitization
  ├─→ Data saved as-is to localStorage
  ├─→ Submitted to backend as-is
  ├─→ Backend should validate/sanitize
  └─→ ⚠️ Frontend accepts any input

SCENARIO 11: User rapidly clicks "Next" button
  ├─→ First click triggers navigation
  ├─→ Subsequent clicks ignored (already navigating)
  ├─→ React Router handles gracefully
  └─→ ✅ No duplicate actions

SCENARIO 12: User's browser blocks localStorage
  ├─→ Zustand persist middleware initialization fails
  ├─→ Store works but data not persisted
  ├─→ Refresh loses all data
  └─→ ⚠️ No warning to user

SCENARIO 13: Backend is slow (30+ seconds)
  ├─→ Axios has default timeout
  ├─→ Eventually throws timeout error
  ├─→ Caught by fallback mechanism
  ├─→ Data saved to fallback storage
  └─→ ✅ No data loss

SCENARIO 14: User enters "Other" but doesn't fill text field
  ├─→ Main field value = "other"
  ├─→ Other text field = "" (empty)
  ├─→ Validation only checks main field
  ├─→ User can proceed
  ├─→ Backend receives: { field: "other", fieldOther: "" }
  └─→ ⚠️ Incomplete data but not blocked

SCENARIO 15: User ranks only 7 of 8 features
  ├─→ phoneFeaturesRanking = { feat1: '1', ..., feat7: '7' }
  ├─→ Object.values() = ['1','2','3','4','5','6','7']
  ├─→ Length = 7 (not 8)
  ├─→ Validation fails
  ├─→ Alert shown, cannot proceed
  └─→ ✅ Validation working correctly

SCENARIO 16: User selects 4 phone functions (max is 3)
  ├─→ UI disables checkboxes after 3 selections
  ├─→ User cannot select 4th
  ├─→ Validation checks length === 3
  └─→ ✅ UI prevents invalid state

SCENARIO 17: Ambassador page skipped, user backs from page 8
  ├─→ Special back logic in handlePrevious()
  ├─→ Checks interestedInAmbassador !== 'yes'
  ├─→ Navigates to page 6 (Ambassador) not page 7
  └─→ ✅ Correct back navigation

SCENARIO 18: User clears browser data mid-survey
  ├─→ localStorage cleared
  ├─→ Zustand store keeps in-memory state
  ├─→ Current session data still available
  ├─→ But refresh would lose everything
  └─→ ⚠️ Data lost on refresh

RECOMMENDED ERROR HANDLING IMPROVEMENTS:

1. Add try-catch around localStorage operations
2. Show warning if localStorage unavailable
3. Add progress indicator during submission
4. Validate "other" text field when "other" selected
5. Add debounce to Next/Submit buttons
6. Implement heartbeat check for multiple tabs
7. Add input sanitization on frontend
8. Add character limits to text fields
9. Show timeout warning on slow networks
10. Add "Save Draft" feature for long surveys

```

---

**End of Data Flow Diagrams Documentation**

*Last Updated: [Current Date]*
*Version: 1.0*



